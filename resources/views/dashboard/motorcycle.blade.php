<x-speedweek-layout><x-slot:title>Mijn motor</x-slot:title><div class='rounded bg-zinc-900 p-6 border border-zinc-800'>@if(! $registration->requiresMotorcycle())<p>Spectator package: geen motor nodig.</p>@else<form method="post" class="grid gap-3 md:grid-cols-2">@csrf @method('patch') @foreach(['brand','model','year','license_plate','vin','front_tire_size','rear_tire_size'] as $f)<input class="rounded bg-zinc-950 border-zinc-700" name="{{ $f }}" placeholder="{{ str_replace('_',' ',$f) }}" value="{{ old($f, $registration->motorcycle?->$f) }}">@endforeach<textarea name="notes">{{ old('notes',$registration->motorcycle?->notes) }}</textarea><button class="rounded-full bg-[#00d1c1] px-4 py-2 font-bold text-black">Save</button></form>@endif</div></x-speedweek-layout>
