<x-speedweek-layout><x-slot:title>Profiel</x-slot:title><div class='rounded bg-zinc-900 p-6 border border-zinc-800'><form method="post" class="grid gap-3 md:grid-cols-2">@csrf @method('patch') @foreach(['first_name','last_name','passport_full_name','country','address','city','postal_code','emergency_contact_name','emergency_contact_phone'] as $f)<input class="rounded bg-zinc-950 border-zinc-700" name="{{ $f }}" placeholder="{{ str_replace('_',' ',$f) }}" value="{{ old($f, $profile?->$f) }}">@endforeach <input type="date" name="date_of_birth" value="{{ old('date_of_birth', optional($profile?->date_of_birth)->format('Y-m-d')) }}"><select name="id_document_type"><option value="passport">Passport</option><option value="id_card">ID card</option></select><textarea name="medical_notes" placeholder="Medical notes">{{ old('medical_notes',$profile?->medical_notes) }}</textarea><button class="rounded bg-amber-400 px-4 py-2 text-zinc-950">Save</button></form></div></x-speedweek-layout>
