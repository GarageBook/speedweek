<x-speedweek-layout><x-slot:title>Reisinfo</x-slot:title><div class='rounded bg-zinc-900 p-6 border border-zinc-800'><form method="post" class="grid gap-3 md:grid-cols-2">@csrf @method('patch')@foreach(['outbound_flight_number','outbound_departure_airport','outbound_arrival_airport','return_flight_number','return_departure_airport','return_arrival_airport','hotel_room_number','bus_group'] as $f)<input class="rounded bg-zinc-950 border-zinc-700" name="{{ $f }}" placeholder="{{ str_replace('_',' ',$f) }}" value="{{ old($f,$registration->travelInfo?->$f) }}">@endforeach<textarea name="notes">{{ old('notes',$registration->travelInfo?->notes) }}</textarea><button class="rounded-full bg-[#00d1c1] px-4 py-2 font-bold text-black">Save</button></form></div></x-speedweek-layout>
