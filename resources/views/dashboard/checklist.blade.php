<x-speedweek-layout><x-slot:title>Checklist</x-slot:title><div class='rounded bg-zinc-900 p-6 border border-zinc-800'><form method="post" class="space-y-2">@csrf @method('patch')@foreach($registration->checklistItems as $item)<label class="block"><input type="checkbox" name="completed[]" value="{{ $item->id }}" @checked($item->completed_at)> {{ $item->checklistItem->title }}</label>@endforeach<button class="rounded-full bg-[#00d1c1] px-4 py-2 font-bold text-black">Save</button></form></div></x-speedweek-layout>
